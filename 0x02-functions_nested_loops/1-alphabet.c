#include "main.h"

/**
 * print_alphabet - prints the alpha in lowercase
 */
void prints_alphabet(void)
{
	char la;

	for (la = 'a'; la <= 'z'; la++)
		_putchar(la);

	_putchar('\n');

	return (0);
}
